<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Checkout Request</title>
    <style>
        body {
            font-family: 'Poppins', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #181C14;
            color: #fff;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #272624;
            border-radius: 8px;
        }
        .header {
            text-align: center;
            padding: 20px;
            background-color: #EBCB00;
            color: #272624;
            border-radius: 8px 8px 0 0;
            margin: -20px -20px 20px -20px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 20px 0;
        }
        .booking-details {
            background-color: rgba(23, 22, 22, 0.5);
            padding: 15px;
            border-radius: 4px;
            margin: 10px 0;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 8px 0;
            padding: 8px 0;
            border-bottom: 1px solid rgba(0, 0, 0, 0.12);
        }
        p, li, span {
            color: #fff;
        }
        .detail-label {
            color: #EBD226;
            font-weight: 500;
        }
        .services-list {
            margin: 15px 0;
        }
        .service-item {
            background-color: rgba(23, 22, 22, 0.3);
            padding: 10px;
            margin: 5px 0;
            border-radius: 4px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid rgba(0, 0, 0, 0.12);
            color: #9E9E9E;
            font-size: 0.875rem;
        }
        .action-required {
            background-color: #FF9800;
            color: #fff;
            padding: 10px;
            border-radius: 4px;
            margin: 15px 0;
            font-weight: 500;
        }
        .user-info {
            background-color: rgba(65, 71, 140, 0.2);
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
        }
        .button-container {
            text-align: center;
            margin: 30px 0;
        }
        .action-button {
            display: inline-block;
            background-color: #EBCB00;
            color: #272624;
            padding: 15px 30px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .action-button:hover {
            background-color: #EBD226;
        }
        /* Ensure good visibility on dark email clients */
        .action-button-fallback {
            display: none;
        }
        @media screen {
            .action-button-fallback {
                display: none !important;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>User Checkout Request</h1>
        </div>
        
        <div class="content">
            <div class="action-required">
                Action Required: Please verify user checkout request
            </div>

            <p>A user has completed their booking time and may be ready to check out.</p>
            
            <div class="user-info">
                <div class="detail-row">
                    <span class="detail-label">User name: </span>
                    <span><%= user.name %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">User contact: </span>
                    <span><%= user.phone || user.email %></span>
                </div>
            </div>
            
            <div class="booking-details">
                <div class="detail-row">
                    <span class="detail-label">Booking ID: </span>
                    <span><%= bookingId %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Parking location: </span>
                    <span><%= parkingLocation %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Parking slot: </span>
                    <span><%= parkingSlot %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Booking date: </span>
                    <span><%= bookingDate %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Start time: </span>
                    <span><%= startTime %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">End time: </span>
                    <span><%= endTime %></span>
                </div>
            </div>

            <% if (services && services.length > 0) { %>
            <div class="services-list">
                <h2 style="color: #EBD226; font-size: 1.25rem;">Additional Services Requested</h2>
                <% services.forEach(function(service) { %>
                    <div class="service-item">
                        <strong><%= service.name %></strong>
                        <p style="margin: 5px 0;"><%= service.description %></p>
                    </div>
                <% }); %>
            </div>
            <% } %>

            <div class="button-container">
                <a href="<%= proccessUrl %>" class="action-button">
                    Verify Checkout Request
                </a>
                <!-- Fallback for email clients that strip buttons -->
                <div class="action-button-fallback">
                    Can't click the button? Copy and paste this link in your browser:
                    <br>
                    <%= process.env.FRONTEND_URL %>/owner/bookings/<%= bookingId %>/checkout
                </div>
            </div>

            <p>Please take the following actions:</p>
            <ol>
                <li>Click the button above to access the checkout verification page</li>
                <li>Locate the user in the specified parking slot</li>
                <li>Verify the parking slot condition</li>
                <li>Process their checkout in the system</li>
            </ol>
            
            <p style="color: #FF9800;">If you cannot locate the user or encounter any issues, please contact our support team immediately.</p>
        </div>

        <div class="footer">
            <p>This is an automated message. Please do not reply to this email.</p>
            <p>© <%= new Date().getFullYear() %> Your Company Name. All rights reserved.</p>
        </div>
    </div>
</body>
</html>